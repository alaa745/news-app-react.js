class ArticleModel {
    constructor(title, author, description, url, urlToImage, publishedAt) {
        this.title = title;
        this.author = author;
        this.description = description;
        this.url = url;
        this.urlToImage = urlToImage;
        this.publishedAt = publishedAt;
    }
}

export default ArticleModel;